package com.webank.wedatasphere.qualitis.service;

/**
 * IMS rule metric service interface — currently inactive.
 *
 * <p>All method declarations have been removed. The interface shell is retained
 * so that existing Spring wiring ({@code ImsRuleMetricServiceImpl}) continues
 * to compile. Re-add method signatures here when the IMS integration is
 * re-enabled.</p>
 *
 * @author v_wenxuanzhang
 */
public interface ImsRuleMetricService {
    // All IMS metric operations are currently disabled.
}
